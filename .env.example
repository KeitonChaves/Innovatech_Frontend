# .env.example — Copia como .env y completa los valores
# NUNCA subas el .env real al repositorio

# URL del backend de Despachos (en producción: IP privada de EC2 backend)
VITE_API_URL=http://localhost:8081
